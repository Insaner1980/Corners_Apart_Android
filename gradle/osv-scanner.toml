[[PackageOverrides]]
ignore = true
reason = "Gradle verification metadata is a checksum trust file, not an application dependency lockfile; Gradle dependencies are scanned by OWASP Dependency-Check."
